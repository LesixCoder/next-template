export enum Themes {
    LIGHT = 'light',
    DARK = 'dark',
}

export enum Environment {
    PC = 'pc',
    IPAD = 'ipad',
    MOBILE = 'mobile',
    NONE = 'none',
}

export enum Language {
    CH = 'ch',
    EN = 'en',
}
